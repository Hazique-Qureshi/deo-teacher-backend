{
  "name": "teacher-management-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for DEO Office Teacher Management System",
  "main": "app/main.py",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --log-level warning",
    "init-db": "python -c \"from app.db.session import engine; from app.models.models import Base; Base.metadata.create_all(bind=engine)\""
  },
  "dependencies": {
    "fastapi": "^0.104.0",
    "uvicorn": "^0.24.0",
    "sqlalchemy": "^2.0.0",
    "supabase": "^2.0.0",
    "python-jose[cryptography]": "^3.3.0",
    "passlib[bcrypt]": "^1.7.4",
    "pydantic-settings": "^2.0.0",
    "python-multipart": "^0.0.6"
  }
}
